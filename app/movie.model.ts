export class Movie {
  constructor(
    public id: number,
    public year: number,
    public title: string,
    public director: string,
    public description: string,
    public image: string
    ) {
  }
}
